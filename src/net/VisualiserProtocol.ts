/**
  * Gets the callgraph packet kind that will
  * map to an expected packet when the callgraph is processing
  */
export const VisualiserPacketKind  = {
  GetVisualiserObject : 'GetVisualiserObject',
  GetVisualiserState : 'GetVisualiserState',
  Error : 'Error',
}
